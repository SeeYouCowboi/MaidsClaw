# Task 4 - Time-Slice Read Accuracy
Generated: 2026-03-27T05:50:53Z

## Baseline (before changes)
bun run build: PASS
bun run check:legacy-memory-surface: PASS
bun test src/memory/navigator.test.ts: PASS (32 tests)
bun test src/memory/stress-time-slice.test.ts: PASS (16 tests)

## Time-Slice Chain Analysis
- `memory_explore` tool resolves `asOfTime + timeDimension` into `asOfCommittedTime` in `src/memory/tools.ts:471-494`.
- `GraphNavigator.explore()` normalizes that input and threads it as `input` into `expandTypedBeam(...)` in `src/memory/navigator.ts:196-217`.
- Beam expansion passes the same `TimeSliceQuery` through `fetchNeighborsByFrontier(...)` to `expandPrivateBeliefFrontier(...)` in `src/memory/navigator.ts:551-625`.
- Before fix, `expandPrivateBeliefFrontier` queried `private_cognition_current` by id/agent only, with no committed-time cutoff.

## Fix Applied
- Added a committed-time SQL predicate in `src/memory/navigator.ts:1044-1055` for private assertion reads:
  - `AND updated_at <= ?` when `timeSlice.asOfCommittedTime` is present.
- This constrains `private_cognition_current` rows used by `expandPrivateBeliefFrontier` to the requested committed-time slice.

## Verification (after changes)
bun run build: PASS
bun test src/memory/navigator.test.ts: PASS (33 tests)
bun test src/memory/stress-time-slice.test.ts: PASS (16 tests)
bun test test/memory/time-slice-query.test.ts: PASS (15 tests)
bun test test/memory/validation-time-model.test.ts: PASS (6 tests)
bun run check:legacy-memory-surface: PASS
lsp_diagnostics src/memory: 0 errors

## QA Scenarios
Scenario 1 (Historical query does not pull current projection by accident): PASS + evidence
- New regression test in `src/memory/navigator.test.ts:595-632` seeds a post-cutoff assertion row (`updated_at=2000`) and an in-slice semantic edge (`created_at=1000`) with `asOfCommittedTime=1500`.
- Result confirms the in-slice semantic path remains available and no post-cutoff edge survives the slice.

Scenario 2 (Boundary timestamp semantics stay stable): PASS + evidence
- Existing boundary/time-slice semantics remain green via:
  - `src/memory/stress-time-slice.test.ts` (16/16)
  - `test/memory/time-slice-query.test.ts` (15/15)
  - `test/memory/validation-time-model.test.ts` (6/6)

## Capability Boundaries Documented
[Area/world current-only getters: confirm test-only, no production fix needed, classified per authority matrix]
- Confirmed unchanged per plan: no time-slice retrofit applied to `area_state_current`, `area_narrative_current`, `world_state_current`, or `world_narrative_current`.
