# Task 10: --mode sync|async flag with latency measurement
# Evidence collected: 2026-04-04

## Build Verification
$ bun run build
$ tsc -p tsconfig.build.json --noEmit
(clean — no errors)

## Changes Applied to scripts/rp-suspicion-test.ts

### 1. CLI Flag: --mode sync|async
- Added `TestMode = "sync" | "async"` type
- Added `mode: TestMode` to `CliOptions`
- `parseArgs()` validates `--mode` value, defaults to "sync"

### 2. Per-Turn Latency Measurement
- `TurnLatencyRecord = { round, talkerLatencyMs, thinkerCompletionMs? }`
- `turnStart = Date.now()` before each turn
- `talkerLatencyMs = Date.now() - turnStart` after each turn
- Average latency calculated and logged after all rounds

### 3. Async Mode: Thinker Polling
- `pollThinkerCompletion(sessionId, sql)` polls PG jobs table every 2s up to 120s
- Queries: `SELECT status FROM jobs WHERE job_type = 'cognition.thinker' AND payload_json->>'sessionId' = $1 ORDER BY created_at DESC LIMIT 1`
- Terminal statuses: completed, reconciled, exhausted
- Falls back gracefully if no PG or no job found

### 4. Async Mode: Config Injection
- `enableTalkerThinkerConfig()` writes config/runtime.json with talkerThinker.enabled: true
- `restoreRuntimeConfig()` restores original file content in finally block
- Backs up original content before modification

### 5. Latency Assertion (Soft Warning)
- avgTalkerLatency < 25000ms threshold
- Logs WARNING if exceeded, does NOT crash test

### 6. Baseline Comparison
- `compareAgainstBaseline()` reads data/rp-test-results/baseline-sync-pre-talker.json
- Compares total score and dimension scores within 10% threshold
- Informational only — baseline file not required to exist

### 7. Output
- Sync mode: unchanged filename behavior
- Async mode: output to data/rp-test-results/async-talker-thinker-5round.json
- Report includes `latency` section with per-turn and average data
- Report includes optional `baseline_comparison` section

### 8. Sync Mode Preservation
- Zero behavioral changes when --mode sync (default)
- All new logic gated behind `opts.mode === "async"` checks

## Files Modified
- scripts/rp-suspicion-test.ts (ONLY file modified, per constraints)

## Verification
- Build: PASS (tsc --noEmit clean)
- No other files modified
- Conversation script, scoring logic, agent config all untouched
