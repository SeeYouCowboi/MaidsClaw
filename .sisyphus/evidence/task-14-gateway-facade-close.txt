Task 14 Evidence — closeSession facade + no manual flush

Command (tool): grep pattern "flushOnSessionClose" in src/gateway/controllers.ts
Result: No matches found

Command (tool): grep pattern "closeSession\(" in src/gateway/controllers.ts
Matches:
568:		const record = await client.closeSession(sessionId);

Conclusion: controllers close path delegates to facade/client closeSession and no manual flush call remains.
