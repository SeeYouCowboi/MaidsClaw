=== RuntimeBootstrapResult still imported in create-app-host.ts ===
3:import type { RuntimeBootstrapResult } from "../../bootstrap/types.js";
66:	_injectedRuntime?: RuntimeBootstrapResult,

=== @internal JSDoc present on RuntimeBootstrapResult ===
 * Full runtime bootstrap result used by the internal composition root.
 * @internal Not intended for public consumption — use
 * {@link PublicRuntimeBootstrapResult} instead when exposing the runtime
 * to consumers outside the bootstrap layer.
 */
export type RuntimeBootstrapResult = {

=== Build passes ===
bun run build: zero errors (tsc -p tsconfig.build.json --noEmit)

=== Full test suite ===
1901 pass, 396 skip, 0 fail across 2297 tests in 171 files
