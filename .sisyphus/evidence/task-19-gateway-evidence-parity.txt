Task: T19 gateway evidence parity
Date: 2026-03-17

Implemented:
- Added gateway evidence endpoints:
  - GET /v1/requests/:request_id/summary
  - GET /v1/requests/:request_id/prompt
  - GET /v1/requests/:request_id/chunks
  - GET /v1/requests/:request_id/diagnose
  - GET /v1/requests/:request_id/trace
  - GET /v1/sessions/:session_id/transcript
  - GET /v1/sessions/:session_id/memory
  - GET /v1/logs
- Wired runtime into gateway controller context for evidence view-model loading.
- Added src/cli/gateway-client.ts with session, streaming turn, and evidence read methods.
- Enabled gateway mode in session create/close/recover.
- Enabled gateway mode in turn send using gateway SSE stream + summary endpoint parity lookup.
- Enabled gateway mode in chat and session shell with gateway client integration.
- Enabled gateway mode in all debug wrappers (summary/transcript/prompt/chunks/logs/memory/trace/diagnose).
- Enforced gateway unsafe-raw rejection path through GatewayClient.rejectUnsafeRaw and debug trace export guard.
- Added test/cli/gateway-mode.test.ts covering:
  - gateway unsafe-raw rejection
  - gateway evidence endpoint structure
  - gateway session/turn command path

Verification:
- bun run build: PASS
- bun test: PASS
