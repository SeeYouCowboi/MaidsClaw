Task: T12 gateway controllers thin-proxy refactor
Date: 2026-03-19

Changed file(s):
- src/gateway/controllers.ts

Verification:
1) Type-check
   Command:
   NODE_OPTIONS="--max-old-space-size=4096" npx tsc -p tsconfig.build.json --noEmit
   Result:
   src/core/models/openai-provider.ts(96,9): error TS2353: Object literal may only specify known properties, and 'status' does not exist in type '{ code: string; message: string; retriable: boolean; details?: unknown; }'.
   Note: matches pre-existing known build error in task context.

2) Gateway regression tests
   Command:
   bun test test/gateway/gateway.test.ts
   Result:
   34 pass, 0 fail

3) CLI gateway-mode regression tests
   Command:
   bun test test/cli/gateway-mode.test.ts
   Result:
   Unhandled pre-existing module resolution error:
   Cannot find module '../../src/cli/commands/debug.js' from 'test/cli/gateway-mode.test.ts'

Behavior evidence:
- controllers now import app-layer clients/services only for inspect/session/health/turn orchestration.
- chunkToGatewayEvent(sessionId, requestId, event) now consumes ObservationEvent.
- runtime Chunk streams are normalized via chunkToObservationEvent/mapChunkStreamToObservations before gateway transport mapping.
- trace endpoint explicitly rejects unsafeRaw/unsafe_raw query usage with INSPECT_UNSAFE_RAW_LOCAL_ONLY message.
