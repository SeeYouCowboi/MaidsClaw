Task 6 evidence — canonicalization core path

Date: 2026-04-19
Repo: D:\ACodingWorkSpace\MaidsClaw

Implemented
- Added thinker-only canonicalization pass in src/runtime/thinker-worker.ts.
- Canonicalization runs after normalizeThinkerAssertionOpsBeforeProjection and before projection commit.
- Projection, slots, relation/conflict handling, and organizer enqueue now consume canonicalizedCognitionOps.

Insertion evidence
- src/runtime/thinker-worker.ts lines ~1381-1394:
  - compute cognitionOps via normalizeThinkerAssertionOpsBeforeProjection(...)
  - compute canonicalizedCognitionOps via canonicalizeThinkerAssertionKeysBeforeProjection(...)
  - commit path uses canonicalizedCognitionOps

Behavior covered
- user-anchored correction rewrites to one canonical key
- no-match preserves original key
- multi-match preserves original key
- terminal/retracted/cross-holder candidates excluded
- weak assertions excluded
- batch-local overlay and later-wins dedupe semantics inside invocation

Verification snapshot
- LSP diagnostics (errors) clean on modified files.
- Targeted tests:
  - thinker-worker-phase2.test.ts: pass
  - thinker-batch-collapse.test.ts: pass for Task 6 scenario after expectation update
  - pipeline-e2e.test.ts: environment failure (missing pg_trgm gin_trgm_ops / node_embeddings table in local PG test DB)
- Build: known unrelated pre-existing error remains in src/gateway/controllers.ts (ErrorCode "UNKNOWN").
