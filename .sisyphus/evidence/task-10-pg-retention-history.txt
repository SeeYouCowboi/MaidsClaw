Evidence: task-10 pg retention — history remains after cleanup
Date: 2026-03-29

## Test: "history remains: terminal current row removed while attempt history stays"

Flow:
1. Enqueue organize job → pending
2. claimNext → running (claim_version=1, job_attempts row inserted)
3. complete → succeeded (terminal_at set)
4. cleanupTerminal(0) → deletes jobs_current row (window=0 means all terminal rows eligible)
5. inspect(job_key) → undefined (jobs_current row gone)
6. getHistory(job_key) → 1 row with outcome='succeeded' (job_attempts intact)

Key verification:
- job_attempts has NO FK to jobs_current
- DELETE FROM jobs_current does NOT cascade to job_attempts
- Audit trail preserved after retention cleanup

## Test: "listExpiredLeases surfaces expired running rows"

Flow:
1. Enqueue + claim with lease_duration_ms=1000
2. listExpiredLeases(baseNow + 100_000) → finds the row (lease expired)
3. Row has status='running' and lease_expires_at < nowMs

## Test output
bun test test/jobs/pg-retention.test.ts
 4 pass, 0 fail, 18 expect() calls
