Task 10: PG Pool Cleanup in shutdown() - Evidence
================================================

Date: 2026-04-01

1. VERIFIED: shutdown() in src/bootstrap/runtime.ts correctly closes PG pool
   - Lines 1078-1082 implement the fire-and-forget pattern:
     ```typescript
     if (pgFactory) {
       void pgFactory
         .close()
         .catch((err) => console.error("PG pool close error:", err));
     }
     ```
   - This keeps shutdown() synchronous while allowing async pool cleanup

2. VERIFIED: PgBackendFactory.close() properly ends the pool
   - Located in src/storage/backend-types.ts lines 91-96
   - Calls this.pool.end() and nullifies the pool reference
   - Is idempotent (safe to call multiple times)

3. LIFECYCLE TEST CREATED: test/pg-app/pg-lifecycle.test.ts
   - Tests pool closes without hanging (5s timeout via Promise.race)
   - Tests idempotent close (calling close() multiple times is safe)
   - Tests getPool() throws after close
   - Tests fire-and-forget pattern used in runtime.ts
   - Uses describe.skipIf(skipPgTests) pattern
   - All 4 tests pass when PG_TEST_URL and PG_APP_TEST_URL are set

4. BUILD STATUS: ✓ bun run build passes (0 errors)

5. TEST STATUS: 
   - New pg-lifecycle.test.ts: 4 tests passing
   - Pre-existing 4 failures in test/runtime/private-thoughts-behavioral.test.ts remain (expected)

6. IMPLEMENTATION CONFIRMED:
   - The shutdown() function correctly closes PG connection pool
   - Fire-and-forget pattern ensures shutdown() stays synchronous
   - No connection leaks when bootstrapRuntime() + shutdown() cycle completes

Commit: fix: add PG pool cleanup to shutdown() for proper connection teardown
