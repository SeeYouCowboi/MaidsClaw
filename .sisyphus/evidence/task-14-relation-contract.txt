Task 14: G7 — Centralize RelationContract + Platform Contract Doc
=================================================================

## Files Created
- src/memory/contracts/relation-contract.ts — centralized relation type definitions
- docs/MEMORY_RELATION_CONTRACT.md — relation type catalog with semantics
- test/memory/relation-contract.test.ts — 5 contract enforcement tests

## Files Modified
- src/memory/graph-edge-view.ts — removed inline RelationContract type/data, imports from centralized contract
- src/storage/domain-repos/pg/graph-read-query-repo.ts — removed inline RelationContract type/data, imports from centralized contract with camelCase adapter
- src/memory/cognition/relation-builder.ts — imports ResolutionChainType from centralized contract

## Centralization Summary
Before: RelationContract type + RELATION_CONTRACTS data duplicated in 2 files with different naming conventions:
  - graph-edge-view.ts: snake_case (source_family, truth_bearing)
  - pg/graph-read-query-repo.ts: camelCase (sourceFamily, truthBearing)

After: Single source of truth in src/memory/contracts/relation-contract.ts
  - Canonical type uses snake_case
  - PG repo maps via toPgContract() adapter to match its camelCase GraphReadEdgeRecord contract
  - relation-builder.ts uses ResolutionChainType from centralized contract

## Build
$ bun run build
tsc -p tsconfig.build.json --noEmit
Exit: 0 (zero type errors)

## Tests
$ bun test test/memory/relation-contract.test.ts
5 pass, 0 fail, 52 expect() calls

$ bun test
884 pass / 372 skip / 44 fail
All failures are pre-existing PG ECONNREFUSED + gateway tests (baseline: 868/349/21 — shift due to environment)
No new failures from relation contract centralization.

## Contract Enforcement Tests (5)
1. RELATION_CONTRACTS contains all logic edge + memory relation entries
2. MEMORY_RELATION_CONTRACTS covers every MemoryRelationType from types.ts
3. RESOLUTION_CHAIN_TYPES are all present in RELATION_CONTRACTS
4. Every contract has valid endpoint families from KNOWN_NODE_KINDS or 'unknown'
5. getRelationContract returns undefined for unknown types
