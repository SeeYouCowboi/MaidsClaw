T6 Storage — Duplicate Migration Evidence
==========================================
Date: 2026-03-08

Test: "error path: duplicate migration step is skipped, not rejected"

Verification:
  1. runMigrations() called with step id "test:dup"
  2. runMigrations() called again with same step
  3. Second call returns [] (empty array — nothing applied)
  4. No exception thrown

Implementation: isMigrationApplied() checks _migrations table before applying.
If migration_id exists, step is silently skipped.
