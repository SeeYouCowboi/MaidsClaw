Time-Slice Contract Doc Verification
=====================================
Date: 2026-04-02

File created: docs/MEMORY_TIME_SLICE_CONTRACT.md

Sections covered:
- Core Concepts: valid_time, committed_time, current_only
- Surface Capability Matrix (table format)
- Per-Surface Detail: area_state, world_state, cognition, episode, search_docs_*, node_embeddings, graph edges
- Tool Layer: memory_explore parameter resolution
- Explicit "NOT supported" declarations (6 items)
- Summary: Capability Boundary (ASCII tree)

Capability findings verified from code:
1. area_state → HISTORICAL via getAreaStateAsOf() [committed_time only, queries area_state_events]
2. world_state → HISTORICAL via getWorldStateAsOf() [committed_time only, queries world_state_events]
3. graph edges → HISTORICAL via filterEvidencePathsByTimeSlice() [both dimensions, post-retrieval]
4. cognition → CURRENT_ONLY [events table has committed_time but no asOf API]
5. episode → CURRENT_ONLY [events table has valid_time + committed_time but no asOf API]
6. search_docs_* → CURRENT_ONLY [no time-slice columns at all]
7. node_embeddings → CURRENT_ONLY [no time-slice columns, upsert overwrites]
