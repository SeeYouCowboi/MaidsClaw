Task 18: G12c — Key Regression Test Assets for PG-native Paths
==============================================================
Date: 2026-04-02
Status: COMPLETE

Inventory of Existing Coverage (BEFORE this task)
-------------------------------------------------
All 5 areas specified in the task already had comprehensive test coverage:

1. Truth schema idempotency:
   - pg-truth-schema.test.ts: 6 tests (incl. explicit idempotency test line 25-29)
   
2. Ops schema idempotency:
   - pg-ops-schema.test.ts: 14 tests (incl. explicit idempotency test line 46-51)

3. Search rebuild PG (search-rebuild-pg.ts):
   - pg-search-rebuild.test.ts: 8 tests (rebuildPrivate, rebuildArea, rebuildWorld,
     rebuildCognition, rebuild('all'), stale doc cleanup, retracted exclusion, agent scoping)
   - test/scripts/search-rebuild-pg.test.ts: 5 additional tests

4. Settlement ledger PG (PgSettlementLedgerRepo):
   - pg-settlement-ledger.test.ts: 13 tests (full lifecycle pending→claimed→applying→applied,
     upsert, conflict, retry, terminal, getByHash, check, idempotent markPending, etc.)

5. Core memory blocks (PgCoreMemoryBlockRepo):
   - pg-memory-blocks-repo.test.ts: 10 tests for core blocks (init, idempotent init, getBlock,
     getBlock missing, append, append reject, read-only reject, replace, replace fail, upsert)
   + 8 tests for PgSharedBlockRepo (create, retrievalOnly, upsert/update sections, getSections,
     deleteSection, renameSection, setTitle, sectionExists, buildSnapshotJson, writeSnapshot,
     CASCADE FK)

6. Pending flush recovery (PgPendingFlushRecoveryRepo):
   - pg-flush-recovery-repo.test.ts: 6 tests (recordPending idempotent, markAttempted,
     queryActive, markResolved, markHardFail, re-record after resolve)

Gaps Identified & Filled
-------------------------
Two micro-gaps were found and addressed in 1 new file:

File: test/pg-app/pg-regression-coverage.test.ts (9 new test cases)

1. Combined schema bootstrap idempotency (2 tests):
   - truth+ops+derived all bootstrapped together, twice — no error
   - Verifies key tables from each layer coexist after double bootstrap

2. Core memory multi-agent isolation (3 tests):
   - Agent A's blocks isolated from agent B (write to A, B stays empty)
   - getAllBlocks returns only the requested agent's blocks
   - replaceBlock on agent A does not affect agent B

3. Settlement ledger concurrent agents (2 tests):
   - Different agents can have independent settlement entries
   - markApplied on one settlement does not affect another

4. Pending flush recovery full retry cycle (2 tests):
   - pending → attempt → attempt → resolve completes cleanly
   - queryActive respects backoff (future next_attempt_at excluded)

Constraints Met
---------------
- New files: 1 (≤5 limit)  ✓
- New test cases: 9 (≤20 limit)  ✓
- All tests skip gracefully without PG: 0 fail, 259 skip  ✓
- Uses describe.skipIf(skipPgTests) pattern  ✓
- No fuzz/stress/business-logic tests  ✓
- No model provider dependency  ✓

Test Run Output
---------------
bun test test/pg-app/ → 0 pass, 259 skip, 0 fail across 24 files [2.09s]
(PG not available in CI — all skip as expected)
