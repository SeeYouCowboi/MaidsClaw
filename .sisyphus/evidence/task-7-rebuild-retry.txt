Task 7 - Search Rebuild Retry Evidence
=======================================
Date: 2026-03-27

search-rebuild.test.ts results:
- 8 tests pass, 0 failures

Test coverage:
1. "recovers search_docs_world after corruption via durable rebuild"
   - Seeds canonical data -> corrupts search_docs_world -> executeSearchRebuild -> verified recovery
2. "recovers search_docs_private for a specific agent after corruption"
   - Seeds private entity -> corrupts search_docs_private -> rebuild -> verified agent-scoped recovery
3. "recovers search_docs_area after corruption"
   - Seeds area event -> corrupts search_docs_area -> rebuild -> verified recovery
4. "scope=all rebuilds all four search_docs tables"
   - Corrupts all four tables -> rebuild with scope=all -> verified all restored
5. "durable persistence: enqueue, claim, execute, complete lifecycle"
   - Full JobPersistence lifecycle: enqueue -> claim -> execute -> complete -> status=reconciled
6. "retry: marks job retryable on transient failure"
   - Enqueue -> claim -> fail(retryable=true) -> status=retryable, attempt_count=1
7. "retry: marks job exhausted after max attempts"
   - Enqueue(maxAttempts=2) -> fail x2 -> status=exhausted, attempt_count=2

Regression check:
- 308 existing memory tests pass (no regressions)
- 3 pre-existing failures in organizer-durable-pipeline.test.ts (unrelated, verified on stash)
- Build error in runtime.ts:498 is pre-existing (verified on stash)
