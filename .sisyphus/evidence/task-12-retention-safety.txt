Task 12: Data Retention + VACUUM — Evidence
============================================

## Files Created
- scripts/memory-maintenance.ts — CLI with --days N, --vacuum, --report
- test/scripts/memory-maintenance.test.ts — 7 tests covering retention safety

## CLI Capabilities
- `--days N`: Delete exhausted/reconciled jobs older than N days (default: 30)
- `--vacuum`: Execute PRAGMA optimize + VACUUM (explicit opt-in only)
- `--report`: Print row counts for all memory tables
- Report-only mode: passing only --report skips cleanup

## Retention Safety
- Only deletes from `_memory_maintenance_jobs` where status IN ('exhausted', 'reconciled')
- CANONICAL_LEDGER_TABLES hardcoded: private_cognition_events, private_episode_events
- These tables have append-only triggers (trg_*_no_update, trg_*_no_delete) in schema
- Retention DELETE never touches canonical tables — verified by test

## Build Verification
- `bun run build`: 0 errors (tsc --noEmit clean)
- `bun test`: 1818 pass, 0 fail (including 7 new tests)

## Test Coverage
1. Retention safety: inserts 10 expired jobs + 2 non-expired, verifies only expired deleted
2. Canonical ledger protection: private_cognition_events count unchanged after retention
3. Status filter: pending/processing/retryable jobs never deleted regardless of age
4. Report: getTableRowCount returns correct counts for known tables
5. Report: getTableRowCount returns null for unknown tables
6. Report: REPORT_TABLES includes all CANONICAL_LEDGER_TABLES
7. Report: CANONICAL_LEDGER_TABLES contains expected table names

## SQL Injection Prevention
- getTableRowCount validates table name against REPORT_TABLES allowlist before query
