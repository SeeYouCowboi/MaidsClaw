Task 14 Regression Fix Evidence

Updated file:
- test/cli/gateway-mode.test.ts

Verification:

1) bun test test/cli/gateway-mode.test.ts
Result:
- 4 pass
- 0 fail

2) bun run build
Result:
- tsc -p tsconfig.build.json --noEmit
- exit 0

Notes:
- GatewayServer in test now uses facade-based options via createLocalAppClients(runtime).
- Test injects a deterministic stub turn facade to keep gateway-mode CLI test stable and independent from agent loop runtime availability.
