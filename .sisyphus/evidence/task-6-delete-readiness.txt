# Delete-Readiness Checklist — Memory V3 Cutover
Generated: 2026-03-27T12:00:00Z

## Intentionally KEPT Compatibility Shims

| Name | Location | Rationale | Plan to Remove |
| --- | --- | --- | --- |
| `legacy:` settlement-id prefix | `src/memory/storage.ts:559` (`settlementId = params.sourceRecordId ?? \`legacy:${params.agentId}:${now}\``) | Backward-compatible settlement ID generation for private events created via the non-explicit (legacy) code path. Removing would break idempotency keys for in-flight private episode events and cognition entries. | Remove when all callers migrate to explicit settlement via `submit_rp_turn` (requires audit of all `createPrivateEvent` call sites). |
| `asOfValidTime` / `asOfCommittedTime` tool support | `src/memory/types.ts:400-401` (MemoryExploreInput), `src/memory/time-slice-query.ts:7-8` (TimeSliceQuery), `src/memory/navigator.ts:1044-1054` (expandPrivateBeliefFrontier) | Backward-compatible time-slice query parameters. These are the canonical V3 time-slice API — not legacy shims per se, but retained for backward compatibility with existing tool callers that use the camelCase field names. | No removal planned — these ARE the canonical V3 API surface. |
| Contested-write safety rejection | `src/memory/task-agent.ts:650-662` (applyCallOneToolCalls, `stance === "contested"` guard) | Prevents the legacy `upsert_assertion` tool from accepting `contested` stance directly. Forces callers to use `submit_rp_turn` with `preContestedStance` and `conflictFactors` for proper contested-write flow. | Remove when the legacy `upsert_assertion` tool path is fully retired (requires all assertion writes to go through explicit settlement). |

## Already Removed Legacy Items

| Name | Removed When | Confirmed By |
| --- | --- | --- |
| `AgentFactOverlay` type | Task 3 (graph relation semantics fix) | grep `AgentFactOverlay` in `src/**/*.ts` returns 0 matches; only reference is in `test/memory/legacy-literal-gate.test.ts` FORBIDDEN_TOKENS list (intentional guard). |
| `agent_fact_overlay` table references in source code | Pre-V3 work (migration dropped table) | grep `agent_fact_overlay` in `src/**/*.ts` returns 0 matches; only reference is in `test/memory/legacy-literal-gate.test.ts` FORBIDDEN_TOKENS list (intentional guard). |
| Dual-read / dual-write overlay pattern | Pre-V3 migration work | No remaining code reads from or writes to `agent_fact_overlay`; all cognition writes go through `CognitionRepository` → `private_cognition_events` + `private_cognition_current`. |

## Items Verified as NOT Present (No Cleanup Needed)

| Candidate | Search Result | Conclusion |
| --- | --- | --- |
| snake_case compat handling in navigator.ts | grep `snake_case\|camelToSnake\|snakeToCamel` in `src/memory/` returns 0 matches | No snake_case conversion shims exist in the memory subsystem. |
| Orphaned types in `src/memory/types.ts` beyond AgentFactOverlay | LSP + grep audit: all exported types have active callers | No dead types found. All 50+ exported types in types.ts are referenced by production or test code. |
