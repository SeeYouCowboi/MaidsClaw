Task T4: Error/Edge Case Evidence
===================================
Date: 2026-03-17

No errors encountered during implementation.

Edge cases tested and verified:
1. Missing config/agents.json → returns { agents: [], diagnostics: [] }
2. Disabled agents (enabled: false) → skipped from loaded profiles
3. RP agent with empty toolPermissions → passes (allow-all semantics)
4. RP agent with non-empty toolPermissions missing submit_rp_turn → diagnostic emitted
5. Duplicate IDs → only first occurrence loaded, diagnostic for second
6. Invalid role → excluded from loaded agents, diagnostic emitted
7. Missing persona ref with personaIds provided → diagnostic
8. Missing persona ref without personaIds → no diagnostic (skip check)
9. Multiple validation errors reported together in single call
10. Role defaults applied correctly for minimal entries

Runtime integration:
- buildAgentRegistry() now accepts runtimeCwd parameter
- existsSync() guard prevents errors when config/agents.json absent
- File agents merge into profile map between presets and explicit options.agentProfiles
  (options.agentProfiles can still override file-backed entries)
