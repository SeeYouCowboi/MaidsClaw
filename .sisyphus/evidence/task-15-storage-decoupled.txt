Task: T15 — storage.ts (GraphStorageService) decouple from bun:sqlite [GAP-C3]
Date: 2026-04-01

## Changes Made to src/memory/storage.ts

1. Removed `import type { Database } from "bun:sqlite"` (line 1)
2. Changed constructor from `(db: Db | Database, ...)` to `(db: Db, ...)`
3. Removed unused `readonly db: Db` public field from GraphStorageService
4. Updated `withDomainRepos()` factory: removed `db` parameter, creates stub Db proxy
5. Removed `normalizeDbInput()` function (lines 1398-1445)
6. Removed `isDb()` function (lines 1447-1449)

File: 1449 → 1400 lines

## Verification

- `bun run build` → 0 errors ✅
- `grep -r "bun:sqlite" src/memory/storage.ts` → 0 matches ✅
- `grep -r "Database" src/memory/storage.ts` → 0 matches (no bun:sqlite Database type) ✅
- LSP diagnostics on storage.ts → 0 errors ✅
- No external consumers of GraphStorageService.db field (verified via AST search + grep) ✅
- All callers (runtime.ts:669, test files) already pass Db, not raw Database ✅

## Additional Fix: task-agent.ts

Prior task had removed normalizeDbInput/isDb from task-agent.ts bottom but functions
were already re-added with RawDatabaseLike type. Verified no duplicates remain.
Build passes clean with all dirty files.

## Architecture Notes

- SqliteGraphStorageLegacyImpl already uses Db interface (not raw Database)
- GraphStorageDelegateRegistry wraps all queries — extraction was already complete
- withDomainRepos() factory now takes only (repoRegistry, jobPersistence?) — no db needed
- Stub Db proxy throws if accidentally accessed, ensuring all access goes through repos
