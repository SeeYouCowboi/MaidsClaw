Task 10: Register Memory Tools in ToolExecutor
================================================

## What was done

1. `registerMemoryTools(executor, services)` called in `src/bootstrap/runtime.ts`
   after `coreMemoryService` creation (line ~1018).

2. An adapter bridges `MemoryToolDefinition` (handler-based) → `ToolDefinition`
   (execute-based) since the two interfaces differ:
   - MemoryToolDefinition.handler(args, viewerContext) 
   - ToolDefinition.execute(params, context)

3. Services provided:
   - coreMemory: coreMemoryService (already created in runtime)
   - retrieval: lazy proxy stub (throws on access — full pipeline not yet wired)
   - navigator, narrativeSearch, cognitionSearch: omitted (optional)

## Registered tools (all 6)

- core_memory_append
- core_memory_replace
- memory_read
- narrative_search
- cognition_search
- memory_explore

## Test results

File: test/bootstrap/memory-tool-registration.test.ts
- 3 tests, 3 pass, 0 fail
- Asserts all 6 tools visible in getSchemas()
- Asserts 4 read-only tools present
- Asserts each schema has name, description, parameters

## Build

- `tsc -p tsconfig.build.json --noEmit` — EXIT 0
- No new LSP diagnostics on changed files
