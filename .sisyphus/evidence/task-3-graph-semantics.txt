# Task 3 - Graph Relation Semantics
Generated: 2026-03-27

## Baseline (before changes)
bun run build: PASS
bun test src/memory/navigator.test.ts: PASS (31/31)
navigator.test.ts:547 ("preserves memory relation semantic kind"): PASS
navigator.test.ts:683 ("conflict_exploration strategy upweights conflicts_with edges"): PASS
bun test src/memory/cognition/memory-relation-types.test.ts: PASS (12/12)
bun run check:legacy-memory-surface: PASS

## Changes Made

### src/memory/types.ts
- Widened `BeamEdge.kind` from `NavigatorEdgeKind` to `NavigatorEdgeKind | MemoryRelationType`

### src/memory/navigator.ts
1. Import: Changed `import type` to `import` to include `MEMORY_RELATION_TYPES` value
2. expandRelationEdges (line 1128): Changed cast from `as NavigatorEdgeKind` (type lie) to `as MemoryRelationType` (truthful — readMemoryRelations only returns MemoryRelationType values)
3. edgePriorityScore (line 1334): Widened parameter from `NavigatorEdgeKind` to `NavigatorEdgeKind | MemoryRelationType`. Added branch: when kind is a MemoryRelationType, returns 0.3 base score instead of 0.1 floor.
4. calculateQueryIntentMatch (line 1492): Widened Set type from `Set<NavigatorEdgeKind>` to `Set<NavigatorEdgeKind | MemoryRelationType>`
5. inferEdgeLayer (line 1734): Widened parameter from `NavigatorEdgeKind` to `NavigatorEdgeKind | MemoryRelationType`

### src/memory/navigator.test.ts
- Added test: "memory relation edge scoring is above floor (0.1) in path scores" — verifies conflicts_with edge under conflict_exploration strategy gets edge_type_score > 0.1

### NOT changed (per MUST NOT rules):
- expandEventFrontier (line ~650): untouched
- expandSemanticEdges (line ~1153): untouched
- GRAPH_RETRIEVAL_STRATEGIES: untouched
- compareNeighborEdges `as MemoryRelationType` casts (lines 1214-1215): preserved

## Verification (after changes)
bun run build: PASS (0 type errors)
bun test src/memory/navigator.test.ts: PASS (32/32, including new test)
bun test src/memory/cognition/memory-relation-types.test.ts: PASS (12/12)
bun run check:legacy-memory-surface: PASS
lsp_diagnostics src/memory: 0 errors (50 files scanned)

## QA Scenarios

### Scenario 1: MemoryRelationType literal survives through BeamEdge.kind to explain output
PASS — navigator.test.ts:547 ("preserves memory relation semantic kind") confirms `supports` literal
appears in `edge.kind` on the output path. The cast is now `as MemoryRelationType` (truthful) instead
of `as NavigatorEdgeKind` (lie). Runtime value was always correct; now type matches.

### Scenario 2: Downstream scoring does NOT penalize memory relation edges to default floor (0.1)
PASS — New test "memory relation edge scoring is above floor" confirms:
- `conflicts_with` edge under `conflict_exploration` has `edge_type_score > 0.1`
- edgePriorityScore returns 0.3 for MemoryRelationType kinds (was 0.1 via indexOf → -1 fallback)
- With strategy multiplier (conflicts_with: 2.0), effective score = 0.3 * 2.0 = 0.6

### Scenario 3: Logic/semantic edge families remain unaffected
PASS — All 31 pre-existing tests pass unchanged, including:
- Logic edge tests (causal, temporal, same_episode)
- Semantic edge tests (semantic_similar)
- Strategy tests (deep_explain beam width, conflict_exploration upweighting)
- QUERY_TYPE_PRIORITY lookup unchanged for NavigatorEdgeKind values
- expandEventFrontier and expandSemanticEdges untouched
