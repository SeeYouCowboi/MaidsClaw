Task: T15 Slash-Dispatcher Mode Branch Elimination
File: src/terminal-cli/shell/slash-dispatcher.ts

Requirement:
- /close handler must call facade.session.closeSession() for unified flush semantics.

Verification:
- grep pattern: facade\.session\.closeSession\(
- match found:
  const closed = await ctx.facade.session.closeSession(ctx.state.sessionId);

Conclusion:
- /close now routes through AppUserFacade.session.closeSession(), inheriting SessionClient flush behavior.
