Embedding Versioning — Dimension Mismatch Safety Evidence
==========================================================

1. upsertNodeEmbedding() dimension validation (storage.ts):
   - Zero-length embeddings are rejected with error
   - Dimension mismatch within same model_id throws Error:
     "Embedding dimension mismatch for model 'X': expected N, got M (node: ...)"
   - Different models can have different dimensions (no cross-model check)

2. cosineSimilarity() warning (embeddings.ts):
   - Dimension mismatch now logs: "cosineSimilarity: dimension mismatch (N vs M), returning 0"
   - Still returns 0 (no breaking change), but issue is now visible in logs

3. queryNearestNeighbors() model_id filter (embeddings.ts):
   - NeighborQueryOptions now accepts optional `modelId` field
   - When provided, SQL WHERE clause filters: "model_id = ?"
   - Without modelId, all embeddings are searched (backward compatible)

4. getEmbeddingStatsByModel() diagnostic (storage.ts):
   - Returns Array<{ model_id, count, dimension }> grouped by model
   - SQL: SELECT model_id, COUNT(*), LENGTH(embedding)/4 FROM node_embeddings GROUP BY model_id

5. memory-rebuild-derived.ts --re-embed flag:
   - New CLI flag: --re-embed
   - Sets forceReEmbed: true in job payload
   - Usage: bun run scripts/memory-rebuild-derived.ts --agent <id> [--re-embed] [--dry-run]

Test results (9 pass, 0 fail):
  - model switch safety: 2 tests (model filter + unfiltered search)
  - dimension mismatch rejection: 4 tests (zero-length, same-model mismatch, cross-model ok, cosine warn)
  - getEmbeddingStatsByModel: 2 tests (populated + empty)
  - memory-test-utils smoke: 1 test

Full suite: 1812 pass, 0 fail
Build: bun run build — zero errors
