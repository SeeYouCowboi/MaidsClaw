Evidence: task-6-pg-enqueue-search-rebuild-idempotent
Date: Sun Mar 29 2026

Test: "search.rebuild same job_key retry does not trigger family coalescing"
Result: PASS

Verified:
  - First enqueue returns { outcome: "created" }
  - Second enqueue with same job_key returns { outcome: "duplicate" }
  - family_state_json remains {} (empty object, unchanged)
  - Only one row exists for the job_key

No family coalescing triggered — that is T7's responsibility.

Test output: 8 pass, 0 fail, 27 expect() calls
