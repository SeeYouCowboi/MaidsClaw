T17: createAppClientRuntime() Bridge Update — Evidence
======================================================

Date: 2026-03-31

## Changes Made

1. src/terminal-cli/app-client-runtime.ts
   - Replaced `bootstrapApp()` with `createAppHost({ role: "local" })`
   - Removed `runtime?: RuntimeBootstrapResult` field from `AppClientRuntime` type
   - Added `@deprecated` JSDoc to `AppClientRuntime` type
   - Function is now async (returns `Promise<AppClientRuntime>`)
   - Gateway mode preserved unchanged
   - Removed unused imports: `join`, `TraceStore`, `bootstrapApp`, `RuntimeBootstrapResult`, `createLocalAppClients`

2. src/terminal-cli/commands/debug.ts
   - `openClientRuntime()` → async, returns `Promise<AppClientRuntime>`
   - `withClientRuntime()` now awaits `openClientRuntime()`

3. src/terminal-cli/commands/session.ts
   - `bootstrapClients()` → async, returns `Promise<AppClientRuntime>`
   - All 3 call sites now await

4. src/terminal-cli/commands/turn.ts
   - `createAppClientRuntime()` call now awaited

5. src/app/host/create-app-host.ts
   - Added `inspectTraceStore` fallback: uses `runtime.traceStore` if available,
     otherwise creates `TraceStore(join(dataDir, "debug", "traces"))` for local/server roles
   - Passes `inspectTraceStore` to `createLocalAppClients()` so inspect queries find traces

## Evidence Checks

### bootstrapApp() removed from app-client-runtime.ts
$ grep -n "bootstrapApp" src/terminal-cli/app-client-runtime.ts
(no output — zero occurrences)

### createAppHost() present
$ grep -n "createAppHost" src/terminal-cli/app-client-runtime.ts
5:import { createAppHost } from "../app/host/index.js";
8: * @deprecated Use `createAppHost()` directly instead. This bridge type exists
33:	const host = await createAppHost({
40:		throw new Error("createAppHost({ role: 'local' }) did not produce a user facade");

### runtime? field removed
$ grep -n "runtime?" src/terminal-cli/app-client-runtime.ts
(no output — field removed)

### @deprecated JSDoc present
$ grep -n "@deprecated" src/terminal-cli/app-client-runtime.ts
8: * @deprecated Use `createAppHost()` directly instead. This bridge type exists

## Build & Test

$ bun run build
tsc -p tsconfig.build.json --noEmit
(clean — zero errors)

$ bun test
2012 pass, 249 skip, 4 fail (all pre-existing), 6437 expect() calls
Ran 2265 tests across 170 files [84.55s]

Pre-existing failures (config-dependent, not related to this change):
- private-thoughts-behavioral.test.ts (4 tests — missing mei persona/agent config)
