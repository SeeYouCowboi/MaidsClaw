Task T9: config show and config write-runtime — Error Log
==========================================================

Date: 2026-03-17

## Build Errors (none from T9)

Only pre-existing error:
  src/cli/commands/agent.ts(370,20): error TS2367: This comparison appears to be
  unintentional because the types 'string' and 'boolean' have no overlap.

This is NOT from T9 changes.

## Test Errors (none)

All 913 tests pass. 0 failures.

## Issues Encountered During Implementation

1. Bun types 1.3.10 lack toHaveProperty in Matchers — used `expect("key" in obj).toBe(true)` instead
2. configStub function was accidentally removed during edit — restored for "doctor" stub
3. Missing closing braces after for loop / function — fixed immediately
