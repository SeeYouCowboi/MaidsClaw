Task 13: Area/World Current Projection Verification
====================================================

Date: 2026-03-27
Surface: area_state_current, world_state_current drift detection

Changes Made
------------
1. scripts/memory-verify.ts — Extended to support --surface area|world|cognition and --all
   - verifyAreaSurface(): compares area_state_current with latest area_state_events per (agent_id, area_id, key)
   - verifyWorldSurface(): compares world_state_current with latest world_state_events per key
   - verifyCognitionSurface(): refactored from original inline code into exported function
   - runVerify(): dispatcher that runs selected surfaces
   - formatVerifyReport(): generates PASS/FAIL report with diff details
   - CLI: --surface area, --surface world, --surface cognition, --all

2. test/scripts/area-world-verify.test.ts — 19 tests covering:
   - Area PASS (consistent dual-write via upsertAreaStateCurrent)
   - Area PASS (multiple updates, latest event wins)
   - Area FAIL (value_json mismatch after manual UPDATE bypass)
   - Area FAIL (missing_from_current: event key deleted from current)
   - Area FAIL (extra_in_current: orphan key inserted directly into current)
   - Area PASS (multi-agent, multi-area independence)
   - World PASS (consistent dual-write)
   - World FAIL (value mismatch after corruption)
   - World FAIL (missing_from_current)
   - World PASS (multiple updates, latest wins)
   - runVerify dispatcher (area only, world only, all three)
   - Overall FAIL when any surface fails
   - formatVerifyReport (PASS and FAIL formatting)

Verification SQL Pattern (area)
-------------------------------
For each (agent_id, area_id, key) in area_state_events:
  - Find latest event by committed_time DESC, id DESC
  - Compare value_json with area_state_current row
  - Missing/extra/mismatched → FAIL

Verification SQL Pattern (world)
--------------------------------
For each key in world_state_events:
  - Find latest event by committed_time DESC, id DESC
  - Compare value_json with world_state_current row
  - Missing/extra/mismatched → FAIL

Build Verification
------------------
$ bun run build   → 0 errors (tsc -p tsconfig.build.json --noEmit)
$ bun test        → 1844 pass, 0 fail

Mismatch Detection Kinds
-------------------------
- value_mismatch: current value_json differs from latest event value_json
- missing_from_current: event key exists but no corresponding current row
- extra_in_current: current row exists with no backing event
