Task T7: config validate — Error Log
======================================

No errors encountered during implementation.

Build: PASS
Tests: 869 pass, 0 fail (2572 expect() calls)

Notes:
- T6 had already created src/cli/commands/config.ts with config init + stubs
- "validate" was in the stub loop — replaced with real handler registration
- No conflicts with T6 work; preserves config init, doctor/show/write-runtime stubs
- readFileSync used instead of readAgentFile to avoid double-parsing and get raw JSON errors
- The handleConfigValidate function is module-private (not exported) — tested via dispatch()
